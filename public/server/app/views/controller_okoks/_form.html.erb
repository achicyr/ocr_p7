<%= form_with(model: controller_okok, local: true) do |form| %>
  <% if controller_okok.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(controller_okok.errors.count, "error") %> prohibited this controller_okok from being saved:</h2>

      <ul>
        <% controller_okok.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
